# VibeForge 环境变量示例
# 复制为 .env.local 并填入你的 Key（.env.local 已被 .gitignore 忽略，不会提交）

# DeepSeek 开放平台申请的 API Key：https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-你的key

# —— Cloudflare Worker 部署时 ——
# 在 Worker「设置 > 变量和机密」里添加同名机密 DEEPSEEK_API_KEY，
# 不需要本地 .env.local；前端 config.js 的 apiUrl 改为你的 worker 地址。
